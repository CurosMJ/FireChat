<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="CustomTheme">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.ActionBar" parent="AppTheme">
    </style>

    <style name="CustomTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorAccent">@color/colorAccent</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/colorTextPrimary</item>
        <item name="android:textColorPrimaryInverse">@color/colorTextPrimaryInverse</item>
        <item name="android:colorBackground">@color/colorBackground</item>
    </style>

    <style name="MessageBox">
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:paddingLeft">8dp</item>
        <item name="android:paddingRight">8dp</item>
    </style>

    <style name="MessageBox.Left">
        <item name="android:background">@drawable/message_bubble_left</item>
        <item name="android:paddingLeft">16dp</item>
    </style>

    <style name="MessageBox.Right">
        <item name="android:background">@drawable/message_bubble_right</item>
        <item name="android:paddingRight">16dp</item>
    </style>

</resources>
